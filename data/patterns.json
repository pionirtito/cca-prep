[
  {
    "id": "deterministic-needs-hook-not-prompt",
    "title": "Deterministicko = hook/kapija, nikad prompt",
    "domain": 1,
    "lesson_sr": "Kad pitanje trazi 'deterministicko' sprovodjenje pravila, odgovor je programski mehanizam (hook, prerequisite gate), NE prompt instrukcija. Prompt ima nenulti procenat greske - model 'povremeno' ne poslusa. Ako pitanje izricito kaze da model ponekad gresi, to je signal da prompt-bazirane opcije otpadaju.",
    "lesson_en": "When a question asks for 'deterministic' enforcement, the answer is a programmatic mechanism (hook, prerequisite gate), NOT a prompt instruction. Prompts have a non-zero failure rate. If the question states the model 'occasionally' misbehaves, that is the tell that prompt-based options are wrong.",
    "seen_in": ["d1-q002", "d1-q008"]
  },
  {
    "id": "intercept-before-not-after",
    "title": "Prevencija hvata PRE akcije, ne posle",
    "domain": 1,
    "lesson_sr": "Da bi SPRECIO stetnu akciju, moras je presresti PRE izvrsenja (PreToolCall). Hvatanje posle (PostToolUse) znaci da je steta vec nastala - to je ciscenje/oporavak, ne prevencija. Uvek proveri KADA mehanizam deluje u odnosu na akciju.",
    "lesson_en": "To PREVENT a harmful action you must intercept BEFORE execution (PreToolCall). Catching it after (PostToolUse) means the damage is already done - that is cleanup/recovery, not prevention. Always check WHEN the mechanism acts relative to the action.",
    "seen_in": ["d1-q002", "d1-q008"]
  },
  {
    "id": "structured-signals-over-text",
    "title": "Oslanjaj se na strukturirane signale, ne na parsiranje teksta",
    "domain": 1,
    "lesson_sr": "Kontrolu toka (npr. kraj agentic petlje) vodi preko strukturiranih signala, nikad preko parsiranja prirodnog jezika iz odgovora. Parsiranje teksta (fraza, XML tag) i fiksni iteration cap-ovi su anti-paterni; cak i stop_sequence je los jer i dalje zavisi od NL generisanja. ALI pazi KOJI strukturni signal: stop_reason je za turn/loop-kontrolu (nastavi dok je tool_use, stani na end_turn), dok je za eksplicitan zavrsetak ZADATKA pravi signal namenski completion tool, ne end_turn (vidi [[task-completion-tool-not-end-turn]]).",
    "lesson_en": "Drive control flow (e.g. agentic loop termination) via structured signals, never by parsing natural language from the response. Text parsing (a phrase, an XML tag) and fixed iteration caps are anti-patterns; even a stop_sequence is poor because it still depends on NL generation. BUT mind WHICH structured signal: stop_reason is for turn/loop control (continue while tool_use, stop on end_turn), whereas for explicit TASK completion the right signal is a dedicated completion tool, not end_turn (see task-completion-tool-not-end-turn).",
    "seen_in": ["d1-q001", "d1-q015"]
  },
  {
    "id": "enforce-dont-disable",
    "title": "Cilj je sprovesti pravilo, ne ugasiti funkciju",
    "domain": 1,
    "lesson_sr": "Resenje koje potpuno uklanja sposobnost (npr. samo read-only tool) je obicno preterano. Politika trazi uslovljen tok rada (dry-run -> odobrenje -> izvrsenje), a ne zabranu. Cuvaj legitimnu funkcionalnost iza kapije, ne brisi je.",
    "lesson_en": "A solution that removes a capability entirely (e.g. read-only only) is usually over-broad. The policy wants a gated workflow (dry-run -> approval -> execute), not a ban. Keep legitimate functionality behind a gate, do not delete it.",
    "seen_in": ["d1-q002"]
  },
  {
    "id": "match-decomposition-to-task-nature",
    "title": "Uskladi dekompoziciju sa prirodom zadatka",
    "domain": 1,
    "lesson_sr": "Strategiju dekompozicije biraj prema PRIRODI zadatka, ne univerzalno. Strukturiran/predvidljiv/ponovljiv zadatak -> fiksni sekvencijalni pipeline (jeftin, determinsticki, pouzdan). Otvoren/kompleksan/varijabilan zadatak -> dinamicka adaptivna dekompozicija (model sam odlucuje korake). Kad sistem ima zadatke razlicite prirode, koristi MESAVINU - i proveri da je raspored tacan. Zamka: opcija sa istim obrascem za sve zadatke pada; opcija sa pravom idejom ali obrnutim rasporedom je najopasniji distraktor.",
    "lesson_en": "Choose the decomposition strategy by the NATURE of the task, not one-size-fits-all. Structured/predictable/repeatable task -> fixed sequential pipeline (cheap, deterministic, reliable). Open-ended/complex/variable task -> dynamic adaptive decomposition (the model decides the steps). When a system has tasks of different natures, use a MIX - and verify the mapping is correct. Trap: the option using one pattern for all tasks fails; the option with the right idea but reversed mapping is the most dangerous distractor.",
    "seen_in": ["d1-q003"]
  },
  {
    "id": "guarantee-with-api-param-not-prompt",
    "title": "Garanciju daje API parametar, ne prompt",
    "domain": 1,
    "lesson_sr": "Kad pitanje trazi da se GARANTUJE / 'always' ponasanje modela, resenje je strukturni parametar u API zahtevu, ne prompt instrukcija. Za iznudjivanje poziva alata to je tool_choice ('any' = mora neki alat, 'tool'+name = mora bas taj). Zamke: temperature kontrolise nasumicnost izlaza, NE tip izlaza (tekst vs tool_use); prompt instrukcija je probabilisticka (model 'uglavnom' poslusa); i pazi na opcije koje izmisljaju nepostojeci 'native feature'. Isti determinsticki koren kao [[deterministic-needs-hook-not-prompt]].",
    "lesson_en": "When a question asks to GUARANTEE / 'always' a model behavior, the answer is a structured parameter in the API request, not a prompt instruction. To force a tool call that is tool_choice ('any' = must use some tool, 'tool'+name = must use that one). Traps: temperature controls output randomness, NOT output type (text vs tool_use); a prompt instruction is probabilistic (the model 'usually' complies); and watch for options inventing a nonexistent 'native feature'. Same deterministic root as deterministic-needs-hook-not-prompt.",
    "seen_in": ["d1-q004"]
  },
  {
    "id": "structured-data-between-agents",
    "title": "Podatke izmedju agenata prenosi kao strukturu, ne kao prozu",
    "domain": 1,
    "lesson_sr": "Kada jedan agent prosledjuje podatke drugom, koristi strukturni format (JSON/CSV), ne 'conversational summary' na prirodnom jeziku. Proza je lossy: gube se tacni brojevi, polja postaju dvosmislena, agent-primalac ne moze pouzdano da rekonstruise vrednosti. Struktura cuva tacne vrednosti i znacenje polja preko granica izmedju agenata. Rodjak [[structured-signals-over-text]] (tamo za kontrolu toka, ovde za prenos podataka).",
    "lesson_en": "When one agent hands data to another, use a structured format (JSON/CSV), not a natural-language 'conversational summary'. Prose is lossy: exact numbers are lost, fields become ambiguous, and the receiving agent cannot reliably reconstruct values. Structure preserves exact values and field semantics across agent boundaries. Sibling of structured-signals-over-text (that one for control flow, this one for data transfer).",
    "seen_in": ["d1-q005"]
  },
  {
    "id": "fix-root-cause-not-symptom",
    "title": "Resi UZROK, ne simptom ('most robust' = ukloni uzrok)",
    "domain": 1,
    "lesson_sr": "Kada pitanje trazi 'most robust' / 'best' arhitektonsko resenje, biraj ono koje uklanja KOREN problema, ne ono koje detektuje ili ublazava posledicu posle. Simptomatska resenja (validacija/provera POSLE akcije, retry petlje, nizenje temperature, dodavanje jos mehanizama odozgo) zvuce korisno ali ostavljaju uzrok netaknut. Posebna zamka: opcija koja kaze 'pre nego sto se prikaze korisniku' zvuci kao prevencija, ali ako samo detektuje gresku koja je vec nastala - to je reaktivno ciscenje, ne prevencija. Srodno sa [[intercept-before-not-after]].",
    "lesson_en": "When a question asks for the 'most robust' / 'best' architectural solution, pick the one that removes the ROOT cause, not one that detects or mitigates the symptom afterward. Symptomatic fixes (validation/checks AFTER the action, retry loops, lowering temperature, bolting on more mechanisms) sound helpful but leave the cause intact. Special trap: an option saying 'before it is shown to the user' sounds like prevention, but if it merely detects an error that already happened, that is reactive cleanup, not prevention. Related to intercept-before-not-after.",
    "seen_in": ["d1-q005"]
  },
  {
    "id": "preserve-reasoning-in-history",
    "title": "Cuvaj reasoning blokove u istoriji (model zna samo messages)",
    "domain": 1,
    "lesson_sr": "Ne izbacuj text/reasoning blokove (chain of thought) iz conversation history radi ustede tokena. Taj blok nije visak - to je logicki most od zahteva do akcije; bez njega model u sledecem krugu vidi DA je pozvao alat ali ne i ZASTO, pa slabije tumaci rezultat. Model je STATELESS: zna iskljucivo ono sto je u messages istoriji; sto je izbaceno - za njega ne postoji i ne regenerise se. Zamka 'besplatna optimizacija': opcija koja tvrdi ustedu 'bez ikakvog uticaja' obicno krije cenu (cesto formulisana 'pola tacno').",
    "lesson_en": "Do not strip text/reasoning blocks (chain of thought) from conversation history to save tokens. That block is not filler - it is the logical bridge from request to action; without it the model sees THAT it called a tool but not WHY, degrading how it interprets the result. The model is STATELESS: it knows only what is in the messages history; anything stripped does not exist for it and is not regenerated. 'Free optimization' trap: an option claiming savings 'with no impact' usually hides a cost (often phrased 'half-true').",
    "seen_in": ["d1-q006"]
  },
  {
    "id": "subagent-stateless-coordinator-passes-context",
    "title": "Subagent je stateless - koordinator prenosi kontekst",
    "domain": 1,
    "lesson_sr": "Kad subagent u kasnijem pozivu 'zaboravi' ono sto je ranije znao/odobrio, uzrok je njegova STATELESS priroda + to sto koordinator nije preneo kontekst - NIKAD neki 'izgubljen/prepisan/keshiran' state. Svaki Task poziv krece od nule. Lek: koordinator mora da u prompt svakog narednog poziva eksplicitno ubaci istoriju prethodnih koraka i relevantne constraint-e. Memorija je odgovornost orkestratora, ne ugradjena funkcija subagenta. Srodno [[structured-data-between-agents]] (taj o FORMATU prenosa, ovaj o ODGOVORNOSTI da se prenese).",
    "lesson_en": "When a subagent 'forgets' in a later invocation what it earlier knew/approved, the cause is its STATELESS nature + the coordinator not passing context - NEVER some 'lost/overwritten/cached' state. Each Task call starts from scratch. Fix: the coordinator must explicitly include the history of prior steps and relevant constraints in the prompt of every subsequent call. Memory is the orchestrator's responsibility, not a built-in subagent feature. Sibling of structured-data-between-agents (that one about the FORMAT of transfer, this one about the RESPONSIBILITY to transfer).",
    "seen_in": ["d1-q007", "d1-q010"]
  },
  {
    "id": "distractor-invents-a-mechanism",
    "title": "Distraktor izmislja mehanizam (ili stvaran termin + pogresan model)",
    "domain": 1,
    "lesson_sr": "Cesta zamka u opcijama: ponudi se precizno IMENOVAN mehanizam/parametar/API pravilo koji zvuci strucno ali NE postoji (npr. 'stateful: true' parametar, automatska regeneracija izbacenog bloka, izmisljeno API ogranicenje 'mora biti praceno text blokom', flag '--new-branch' koji opisuje koncept ali nije stvaran primitiv dok se forkovanje radi preko 'fork_session'). Varijanta: koristi se STVARAN termin (npr. 'fork-based session') ali sa pogresnim mentalnim modelom problema. Provod: pre nego sto poverujes 'magicnom' resenju, pitaj postoji li taj mehanizam uopste i da li opisani model odgovara onome sto pitanje stvarno opisuje. Srodno [[guarantee-with-api-param-not-prompt]] (tamo: pravo resenje JESTE realan API parametar; ovde: distraktor IZMISLJA parametar).",
    "lesson_en": "A common trap in options: a precisely NAMED mechanism/parameter/API rule that sounds technical but does NOT exist (e.g. a 'stateful: true' parameter, automatic regeneration of a stripped block, an invented API constraint 'must be preceded by a text block', a '--new-branch' flag that describes the concept but is not a real primitive while forking is done via 'fork_session'). Variant: a REAL term (e.g. 'fork-based session') applied with the wrong mental model of the problem. Defense: before believing a 'magic' fix, ask whether the mechanism even exists and whether its described model matches what the question actually describes. Related to guarantee-with-api-param-not-prompt (there: the right answer IS a real API parameter; here: the distractor INVENTS one).",
    "seen_in": ["d1-q006", "d1-q007", "d1-q010", "d1-q016", "d1-q018"]
  },
  {
    "id": "match-hook-to-job",
    "title": "Hook biraj prema POSLU, ne reputaciji (Pre vs Post)",
    "domain": 1,
    "lesson_sr": "Izbor izmedju PreToolCall i PostToolUse hook-a zavisi od POSLA, ne od toga koji je 'bolji'. Treba SPRECITI stetnu/nedozvoljenu akciju -> PreToolCall (presretni PRE izvrsenja, blokiraj). Treba OBRADITI/normalizovati nepouzdan ili sirov REZULTAT (retry, backoff, ciscenje, standardizovan fallback) -> PostToolUse (presretni REZULTAT POSLE izvrsenja). Tvrdnja 'PostToolUse je reaktivan/los' vazi SAMO kad je cilj prevencija; kad je cilj graciozno rukovanje rezultatom, PostToolUse je bas pravi alat. Zamka: pitanje odmah posle 'PreToolCall je tacan' pitanja cesto ubaci PreToolCall distraktor da te namami da slepo ponovis obrazac. Dopunjuje [[intercept-before-not-after]] (taj: 'za prevenciju -> pre'; ovaj dodaje 'za obradu rezultata -> posle').",
    "lesson_en": "Choosing between a PreToolCall and a PostToolUse hook depends on the JOB, not on which is 'better'. Need to PREVENT a harmful/disallowed action -> PreToolCall (intercept BEFORE execution, block). Need to HANDLE/normalize an unreliable or raw RESULT (retry, backoff, cleanup, standardized fallback) -> PostToolUse (intercept the RESULT AFTER execution). The claim 'PostToolUse is reactive/bad' holds ONLY when the goal is prevention; when the goal is graceful result handling, PostToolUse is exactly the right tool. Trap: a question right after a 'PreToolCall is correct' question often plants a PreToolCall distractor to bait you into blindly repeating the pattern. Complements intercept-before-not-after (that one: 'to prevent -> before'; this one adds 'to handle results -> after').",
    "seen_in": ["d1-q008", "d1-q009"]
  },
  {
    "id": "handle-errors-outside-model-loop",
    "title": "Transport-greske i retry drzi van modelove petlje",
    "domain": 1,
    "lesson_sr": "Tranzijentne greske na transport nivou (503, timeout) i logiku ponavljanja (retry, exponential backoff) drzi u deterministickom kodu (hook/orkestrator), van modelove petlje rezonovanja. Tri razloga: (1) cuva token kontekst - model ne vidi neuspele pokusaje, samo finalni ishod; (2) sprecava halucinaciju - model nikad ne dobije sirov error da ga pogresno protumaci ili da 'pogadja' podatke; (3) pouzdanije i robusnije od prompt-baziranog retry-ja (koji je probabilistican i krhak). Modelu vrati ili finalne podatke ili standardizovan signal stanja (graceful degradation). Srodno [[match-hook-to-job]] (PostToolUse je mesto gde ovo zivi).",
    "lesson_en": "Keep transport-level transient errors (503, timeout) and retry logic (retry, exponential backoff) inside deterministic code (hook/orchestrator), outside the model's reasoning loop. Three reasons: (1) preserves token context - the model never sees failed attempts, only the final outcome; (2) prevents hallucination - the model never receives a raw error to misinterpret or to 'guess' data around; (3) more reliable and robust than prompt-based retry (which is probabilistic and brittle). Return to the model either finalized data or a standardized state signal (graceful degradation). Related to match-hook-to-job (PostToolUse is where this lives).",
    "seen_in": ["d1-q009", "d1-q011"]
  },
  {
    "id": "coordinator-gates-not-peer-to-peer",
    "title": "Subagente vezuj preko koordinatora (hub-and-spoke), ne direktno",
    "domain": 1,
    "lesson_sr": "Kad jedan subagent zavisi od rezultata drugog, NE vezuj ih direktno (peer-to-peer) - to je krhko: nema sloja koji proverava da li je upstream korak uspeo, pa greska jednog tiho zatruje drugog (npr. downstream agent halucinira iz praznog/nepotpunog ulaza) i debug postaje pakao. Lek: hub-and-spoke / koordinator - sve ide kroz centralni hub koji centralizuje error handling, validaciju i retry/backoff, i poziva downstream SAMO na uspeh upstream-a. Greska se hvata na granici, ne unutar agenta koji halucinira. Zamke: monolitni merge (zrtvuje modularnost/fault isolation, 'halt' nije principled error handling); shared memory bus + polling (overengineering, coupling, busy-wait, ne resava validaciju); davanje downstream agentu da parsira tudje logove (krsi separation of concerns). Ovo je isti 'obradu gresaka drzi van agenta koji radi posao' koren kao [[handle-errors-outside-model-loop]] (tamo na nivou hook-a, ovde na nivou orkestracije), i dopunjuje [[subagent-stateless-coordinator-passes-context]] (koordinator nije samo prenosilac konteksta nego i kapija toka).",
    "lesson_en": "When one subagent depends on another's result, do NOT wire them directly (peer-to-peer) - it is brittle: there is no layer checking whether the upstream step succeeded, so one agent's failure silently poisons the next (e.g. the downstream agent hallucinates from empty/incomplete input) and debugging becomes a nightmare. Fix: hub-and-spoke / coordinator - everything routes through a central hub that centralizes error handling, validation, and retry/backoff, and invokes the downstream agent ONLY on upstream success. Errors are caught at the boundary, not inside the agent that hallucinates. Traps: monolithic merge (sacrifices modularity/fault isolation, 'halt' is not principled error handling); shared memory bus + polling (over-engineering, coupling, busy-wait, does not solve validation); letting the downstream agent parse the other's logs (violates separation of concerns). Same 'keep error handling outside the agent doing the work' root as handle-errors-outside-model-loop (there at the hook level, here at the orchestration level), and complements subagent-stateless-coordinator-passes-context (the coordinator is not just a context carrier but also a flow gate).",
    "seen_in": ["d1-q011"]
  },
  {
    "id": "structured-handoff-not-raw-dump",
    "title": "Predaja je strukturisana sinteza, ne sirov dump",
    "domain": 1,
    "lesson_sr": "Kod predaje zadatka (eskalacija ka coveku ili agent->agent) primalac treba KURIRANU, STRUKTURISANU predaju: uzrok + sta je pokusano + preporucene sledece akcije - da nastavi, ne da krene iz nule. Najpouzdaniji nacin da se to iznudi je tool sa OBAVEZNIM poljima (schema primorava agenta da sintetizuje bas potreban kontekst). Tri zamke: (1) SIROV DUMP - zalepi transkript / poslednjih N poruka; vise podataka != upotrebljiv kontekst, primalac i dalje rekonstruise, nema analize/preporuka; (2) POGRESAN AKTER - prebaci sazimanje na korisnika; lossy i laicki, korisnik nema dijagnosticki kontekst koji agent ima; (3) POGRESAN PROBLEM - HITL/odobravanje svake akcije resava kontrolu/bezbednost, ne kvalitet predaje, plus dodaje latenciju. Predaja = destilovana odluka, ne arhiva. Linkuje [[structured-data-between-agents]] (taj o agent->agent FORMATU; ovaj o SINTEZI/predaji ka coveku) i [[guarantee-with-api-param-not-prompt]] (obavezna polja = struktura iznuduje kompletnost).",
    "lesson_en": "When handing off a task (escalation to a human, or agent->agent), the receiver needs a CURATED, STRUCTURED handoff: the cause + what was attempted + recommended next actions - so they continue rather than start from scratch. Best enforced via a tool with REQUIRED fields (the schema forces the agent to synthesize exactly the needed context). Three traps: (1) RAW DUMP - append the transcript / last N messages; more data != usable context, the receiver still has to reconstruct, no analysis/recommendations; (2) WRONG ACTOR - offload the summary to the user; lossy and lay, the user lacks the diagnostic context the agent has; (3) WRONG PROBLEM - HITL/approving every action solves control/safety, not handoff quality, and adds latency. A handoff is a distilled decision, not an archive. Links structured-data-between-agents (that one about agent->agent FORMAT; this one about SYNTHESIS/handoff to a human) and guarantee-with-api-param-not-prompt (required fields = structure enforcing completeness).",
    "seen_in": ["d1-q012"]
  },
  {
    "id": "decompose-overloaded-agent-not-scale-it",
    "title": "Preopterecen agent se DEKOMPONUJE, ne skalira",
    "domain": 1,
    "lesson_sr": "Kad jedan agent zongliranjem previse zadataka/tool-ova istovremeno upadne u cognitive overload (zbunjenost, ispusteni zadaci, prekoraceni reasoning limiti), lek je DEKOMPOZICIJA na coordinator-subagent: koordinator rastavi posao, delegira svaki deo specijalizovanom subagentu sa fokusiranim kontekstom i jednim poslom, pa agregira nalaze (manji reasoning load po agentu, nema tool-selection zabune, paralelno izvrsenje). Bottleneck je ORKESTRACIJA/kognitivni teret, ne kapacitet ni brzina. Zamke koje napadaju pogresnu polugu: veci token window (tretira kao manjak prostora - daje vise sobe da se zbuni, bez strukture); manji/brzi model (pogorsava - slabije rezonovanje na istom poslu); peer-to-peer glasanje (zvuci distribuirano ali ne smanjuje teret po zadatku, dodaje coordination overhead, krhko bez huba). Rastereti kroz PODELU, ne kroz 'daj mu vise/brze'. Linkuje [[coordinator-gates-not-peer-to-peer]] (taj o topologiji/gejtovanju; ovaj o rasterecenju kroz podelu) i [[match-decomposition-to-task-nature]] (taj bira KOJU dekompoziciju; ovaj kaze da DEKOMPONUJES umesto da skaliras isti agent).",
    "lesson_en": "When a single agent juggling too many tasks/tools at once falls into cognitive overload (confusion, dropped tasks, exceeded reasoning limits), the fix is DECOMPOSITION into coordinator-subagent: the coordinator splits the work, delegates each part to a specialized subagent with focused context and one job, then aggregates findings (lower reasoning load per agent, no tool-selection confusion, parallel execution). The bottleneck is ORCHESTRATION/cognitive load, not capacity or speed. Traps that pull the wrong lever: bigger token window (treats it as lack of room - gives more space to get confused, no structure); smaller/faster model (makes it worse - weaker reasoning on the same work); peer-to-peer voting (sounds distributed but does not reduce per-task burden, adds coordination overhead, brittle without a hub). Relieve via DIVISION, not via 'give it more/faster'. Links coordinator-gates-not-peer-to-peer (that one about topology/gating; this one about relief through division) and match-decomposition-to-task-nature (that one picks WHICH decomposition; this one says DECOMPOSE rather than scale the same agent).",
    "seen_in": ["d1-q013"]
  },
  {
    "id": "chunk-local-then-integrate-global",
    "title": "Chunking vraca lokalno ali gubi globalno -> dodaj reduce (integration) pass",
    "domain": 1,
    "lesson_sr": "Seckanje dugackog ulaza na chunk-ove vraca LOKALNU tacnost (izbegava attention dilution jednog ogromnog prompta), ali GUBI globalni/cross-chunk pregled - problemi koji se protežu preko vise chunk-ova (npr. narativna doslednost preko celog videa) ispadaju jer nijedan pojedinacni chunk ne vidi ostale. Lek: dodaj drugi-stepeni integration/synthesis (reduce) pass koji rezonuje nad SAZECIMA/izlazima lokalnih prolaza -> hvata long-range obrasce, a bez povratka dilution-a (jer integraciji ulaze sazeci, ne sirov ceo ulaz). To je Map-Reduce / Synthesis pattern: map = lokalni prolazi, reduce = integracija. Zamke: vracanje na jedan prompt (cak i sa strogim XML tagovima) = nazad u attention dilution (tagovi ne smanjuju kolicinu konteksta); 'map-reduce BEZ finalnog integration koraka' = samo paralelizam, izostavlja bas reduce koji JESTE resenje; biranje koje chunk-ove citati (dinamicka adaptacija) = i dalje lokalna odluka, nema agregacionog sloja; temperature nema veze sa opsegom problema. Pouka: razliciti OPSEZI (lokalni vs globalni) traze razlicite SLOJEVE analize. Linkuje [[match-decomposition-to-task-nature]] (taj bira strategiju dekompozicije po prirodi zadatka; ovaj resava lokalno/globalno kompromis chunking-a kroz reduce sloj).",
    "lesson_en": "Chunking a long input restores LOCAL accuracy (avoids the attention dilution of one giant prompt) but LOSES the global/cross-chunk view - issues that span multiple chunks (e.g. narrative consistency across a whole video) slip through because no single chunk sees the others. Fix: add a second-stage integration/synthesis (reduce) pass that reasons over the SUMMARIES/outputs of the local passes -> it catches long-range patterns without bringing back dilution (the integration consumes summaries, not the raw whole input). This is the Map-Reduce / Synthesis pattern: map = local passes, reduce = integration. Traps: reverting to a single prompt (even with strict XML tags) = back to attention dilution (tags do not reduce the amount of context); 'map-reduce WITHOUT a final integration step' = just parallelism, omitting the very reduce that IS the fix; choosing which chunks to read (dynamic adaptation) = still a local decision, no aggregation layer; temperature has nothing to do with the scope of the problem. Lesson: different SCOPES (local vs global) require different LAYERS of analysis. Links match-decomposition-to-task-nature (that one picks the decomposition strategy by task nature; this one resolves the local/global tradeoff of chunking via a reduce layer).",
    "seen_in": ["d1-q014"]
  },
  {
    "id": "task-completion-tool-not-end-turn",
    "title": "Zavrsetak ZADATKA signalizira completion tool, ne end_turn",
    "domain": 1,
    "lesson_sr": "Da signalizira da je ZADATAK gotov (a ne samo da je jedan potez zavrsen), koristi NAMENSKI completion tool - model emituje tool_use blok (npr. research_complete()), a petlja ga detektuje po imenu. NE oslanjaj se na stop_reason == end_turn: end_turn znaci da je model zavrsio TAJ POTEZ (moze ga zavrsiti i da saceka rezultate alata ili korisnikov unos), pa NE nosi semantiku zavrsetka zadatka i ugasio bi petlju prerano. Parsiranje prirodnog jezika (fraza 'Research complete:', XML tag) i stop_sequence (</research_complete>) su takodje pogresni - svi zavise od toga da model emituje tacan tekst, nisu deterministicka kontrola. KLJUCNA RAZLIKA: TURN-completion (end_turn, kontrola jednog kruga petlje) != TASK-completion (completion tool, semantika 'posao gotov'). NAJOPASNIJI TRAP: pitanje izgleda kao obicno loop-kontrola pitanje (gde je end_turn tacan), pa instinktivno biras end_turn - a pita se zavrsetak zadatka. Linkuje [[structured-signals-over-text]] (taj: ne parsiraj NL, koristi strukturni signal; ovaj dodaje KOJI strukturni signal - tool poziv za task-completion, ne end_turn).",
    "lesson_en": "To signal that a TASK is complete (not merely that one turn ended), use a DEDICATED completion tool - the model emits a tool_use block (e.g. research_complete()) which the loop detects by name. Do NOT rely on stop_reason == end_turn: end_turn means the model finished THAT TURN (it may end a turn to await tool results or user input), so it does NOT carry task-completion semantics and would terminate the loop prematurely. Natural-language parsing (a 'Research complete:' phrase, an XML tag) and stop_sequence (</research_complete>) are also wrong - all depend on the model emitting exact text, not deterministic control. KEY DISTINCTION: TURN-completion (end_turn, single-loop-turn control) != TASK-completion (completion tool, 'job done' semantics). MOST DANGEROUS TRAP: the question looks like an ordinary loop-control question (where end_turn IS correct), so you instinctively pick end_turn - but it asks about task completion. Links structured-signals-over-text (that one: don't parse NL, use a structured signal; this one adds WHICH structured signal - a tool call for task-completion, not end_turn).",
    "seen_in": ["d1-q015"]
  },
  {
    "id": "route-by-complexity-not-fixed-pipeline",
    "title": "Rutiraj po slozenosti (fast path), ne provlaci sve kroz fiksni pipeline",
    "domain": 1,
    "lesson_sr": "Kad fiksni pipeline provlaci SVAKI upit kroz sve subagente (skupo i sporo za proste slucajeve - npr. prost upit ide kroz sve specijaliste pre sinteze), lek je dinamicko RUTIRANJE/triage: Coordinator unapred proceni slozenost/nameru i posalje upit SAMO relevantnom subagentu ('fast path'), bypass ostalih -> uklanja nepotrebne pozive na izvoru, smanjuje I latenciju I compute, a tacnost ostaje. Zamke: (1) 'broadcast svima + prvi odgovor pobedjuje' = brze na papiru ALI zrtvuje tacnost (prvi odgovor != tacan u QA; specijalizovan agent moze vratiti generican/nepotpun odgovor brze; nedeterminizam) i i dalje trosi sve subagente, ne dira koren; (2) jaci hardver = kapacitet ne leci ARHITEKTURU (pipeline i dalje vrti sve nepotrebne pozive); (3) mikro-optimizacija pojedinog koraka (subagent brze vrati prazan string) = optimizujes otpad umesto da ga UKLONIS. SIRA POUKA: cuvaj se opcije koja optimizuje bas TRAZENU metriku (latenciju) ali zrtvuje tacnost - 'brzo ali pogresno'. Linkuje [[match-decomposition-to-task-nature]] (taj bira fiksnu/dinamicku dekompoziciju po prirodi zadatka; ovaj kaze rutiraj po slozenosti umesto fiksnog punog pipeline-a) i [[decompose-overloaded-agent-not-scale-it]] (oba: resi STRUKTURU, ne bacaj kapacitet/hardver).",
    "lesson_en": "When a fixed pipeline pushes EVERY query through all subagents (expensive and slow for simple cases - e.g. a trivial query traverses all specialists before synthesis), the fix is dynamic ROUTING/triage: the Coordinator evaluates complexity/intent upfront and sends the query to ONLY the relevant subagent ('fast path'), bypassing the rest -> eliminates unnecessary calls at the source, cutting both latency and compute while preserving accuracy. Traps: (1) 'broadcast to all + take the first response' = fast on paper BUT sacrifices accuracy (first response != correct in QA; a specialized agent may return a generic/incomplete answer faster; non-determinism) and still spends all subagents, not addressing the root; (2) faster hardware = capacity does not cure ARCHITECTURE (the pipeline still runs all the unnecessary calls); (3) micro-optimizing a single step (a subagent returns an empty string faster) = optimizing the waste instead of removing it. BROADER LESSON: beware the option that optimizes the very metric asked for (latency) while sacrificing accuracy - 'fast but wrong'. Links match-decomposition-to-task-nature (that one picks fixed/dynamic decomposition by task nature; this one says route by complexity instead of a fixed full pipeline) and decompose-overloaded-agent-not-scale-it (both: fix the STRUCTURE, don't throw capacity/hardware at it).",
    "seen_in": ["d1-q017"]
  },
  {
    "id": "human-handoff-synthesize-not-share-session",
    "title": "Predaja izmedju LJUDI: izoluj scope + sintetizuj znanje (ne deli/forkuj zivu sesiju)",
    "domain": 1,
    "lesson_sr": "Za paralelni rad / predaju IZMEDJU LJUDSKIH developera, CCA-F preporuka je: IZOLUJ scope (zasebna/nova sesija po coveku, Git grane po zadatku da se izbegnu konflikti nad istim fajlovima) + prenesi znanje Claude-generisanim SAZETKOM/kontekstom (Projects-stil deljeno znanje, koristi Claude-ovu sintezu). Claude Code NE podrzava: vise ljudi u istoj sesiji istovremeno ('concurrent multi-user prompt resolution' - izmisljeno), JSON export/email/import session istorije (nedokumentovano; persistencija = Git commit + progress notes), niti fork_session koji grana ZIVO interaktivno stanje izmedju ljudi. KLJUCNA TENZIJA/DISKRIMINATOR sa d1-q016 ([[ fork-based session ]]): u q016 je fork_session bio tacan kao APSTRAKTNI session-state primitiv ('koja operacija forkuje'); u q018 je fork_session NETACAN za handoff IZMEDJU DVOJE LJUDI. Kada pitanje kaze 'predaj drugom developeru / paralelni LJUDSKI rad', biraj summarize+isolate (Projects/Git), NE forkovanje/deljenje sesije. NAPOMENA: ovo je mesto gde kviz deluje protivrecno - razlikuj po kontekstu (apstraktni primitiv vs saradnja dvoje ljudi). Linkuje [[distractor-invents-a-mechanism]] (A concurrent multi-user i B JSON export/import su izmisljeni mehanizmi).",
    "lesson_en": "For parallel work / handoff BETWEEN HUMAN developers, the CCA-F recommendation is: ISOLATE scope (a separate/new session per person, Git branches per task to avoid conflicts on the same files) + transfer knowledge via a Claude-generated SUMMARY/context (Projects-style shared knowledge, leveraging Claude's synthesis). Claude Code does NOT support: multiple humans in the same session concurrently ('concurrent multi-user prompt resolution' - invented), JSON export/email/import of session history (undocumented; persistence = Git commits + progress notes), nor a fork_session that branches LIVE interactive state between humans. KEY TENSION/DISCRIMINATOR with d1-q016 (fork-based session): in q016 fork_session was correct as an ABSTRACT session-state primitive ('which operation forks'); in q018 fork_session is INCORRECT for handoff BETWEEN TWO HUMANS. When a question says 'hand off to another developer / parallel HUMAN work', pick summarize+isolate (Projects/Git), NOT forking/sharing a session. NOTE: this is where the quiz appears self-contradictory - discriminate by context (abstract primitive vs two-human collaboration). Links distractor-invents-a-mechanism (A concurrent multi-user and B JSON export/import are invented mechanisms).",
    "seen_in": ["d1-q018"]
  },
  {
    "id": "batch-parallel-tool-results-one-user-message",
    "title": "Paralelni tool_result-i idu u JEDNU user poruku (Messages API alternacija)",
    "domain": 1,
    "lesson_sr": "Messages API zahteva STROGU naizmenicnost user/assistant role-ova - nema dve uzastopne poruke istog role-a. U agentic petlji: tool_use blokovi su u ASSISTANT poruci; pripadajuci tool_result blokovi u SLEDECOJ USER poruci, upareni preko tool_use_id. Kad asistent emituje VISE tool_use blokova u jednom potezu (parallel tool use), svi tool_result-i moraju nazad ZAJEDNO u JEDNU user poruku (vise blokova u jednoj poruci) - NE u vise zasebnih user poruka (to su uzastopne user poruke -> krsi alternaciju). Zamke: tool_result u SYSTEM poruci (pogresan sloj - system je za instrukcije, postavlja se jednom; rezultati idu u user); izmisljena ASSISTANT 'potvrda' izmedju rezultata (protokol to ne trazi; poremetilo bi uparivanje); disable_parallel_tool_use (parallel JE podrzan - problem je PAKOVANJE rezultata, ne paralelizam). Sibling [[preserve-reasoning-in-history]] (oba o ispravnom konstruisanju messages niza u agentic petlji - taj cuva reasoning blokove, ovaj pravilno pakuje tool_result-e).",
    "lesson_en": "The Messages API requires STRICT user/assistant role alternation - no two consecutive same-role messages. In the agentic loop: tool_use blocks live in the ASSISTANT message; the corresponding tool_result blocks live in the NEXT USER message, paired by tool_use_id. When the assistant emits MULTIPLE tool_use blocks in one turn (parallel tool use), all tool_results must be returned TOGETHER in ONE user message (multiple blocks in one message) - NOT as several separate user messages (those are consecutive user messages -> breaks alternation). Traps: putting tool_result in a SYSTEM message (wrong layer - system is for instructions, set once; results go in user); inventing an ASSISTANT 'acknowledgment' between results (the protocol does not require it; it would break the pairing); disable_parallel_tool_use (parallel IS supported - the flaw is the PACKAGING of results, not parallelism). Sibling of preserve-reasoning-in-history (both about constructing the messages array correctly in the agentic loop - that one preserves reasoning blocks, this one packs tool_results correctly).",
    "seen_in": ["d1-q019"]
  },
  {
    "id": "fork-to-isolate-context",
    "title": "Forkuj posle zajednickog setupa da izolujes kontekst (sprecava context pollution)",
    "domain": 1,
    "lesson_sr": "Kad vise nezavisnih istraga/grana dele zajednicki pocetni setup ali bi medjusobno ZAGADILE kontekst (irelevantni podaci jedne istrage cure u drugu i zbunjuju model = context pollution), lek je FORKOVATI sesiju POSLE zajednickog koraka u izolovane niti: svaka grana NASLEDI kopiju zajednickog stanja (npr. pocetni logovi), pa dalje ima SOPSTVENI context window -> sum jedne istrage ne curi u drugu, a deljeni setup se ne gubi. Zamke: brisanje konteksta (ClearContext) = DESTRUKTIVNO, gubi i zajednicki setup koji svima treba; strukturni JSON/atribucija u JEDNOJ niti = kontekst i dalje zagadjen (atribucija != izolacija); paralelni Task pozivi/zasebni subagenti = TAKODJE izoluju ALI to je SPAWNING subagenata, ne fork-based session (kad pitanje IMENUJE fork, koristi fork; i subagentima bi trebalo eksplicitno proslediti setup [[subagent-stateless-coordinator-passes-context]], dok grana nasledjuje automatski). Fork je ovde TACAN (apstraktna session-state izolacija, kao d1-q016) - za razliku od [[human-handoff-synthesize-not-share-session]] (d1-q018) gde je fork pogresan jer je predaja IZMEDJU LJUDI; kontekst (apstraktni primitiv vs ljudska saradnja) odlucuje. META-pouka: kad pitanje IMENUJE konkretan mehanizam, biraj opciju koja koristi BAS taj, ne alternativu koja samo 'takodje radi'.",
    "lesson_en": "When several independent investigations/branches share a common initial setup but would POLLUTE each other's context (irrelevant data from one bleeding into another and confusing the model = context pollution), the fix is to FORK the session AFTER the shared step into isolated threads: each branch INHERITS a copy of the shared state (e.g. the initial logs), then has its OWN context window -> one investigation's noise does not leak into the other, and the shared setup is not lost. Traps: clearing context (ClearContext) = DESTRUCTIVE, also loses the shared setup both need; structured JSON/attribution in a SINGLE thread = context still polluted (attribution != isolation); parallel Task calls/separate subagents = also isolate BUT that is subagent SPAWNING, not fork-based session (when the question NAMES fork, use fork; subagents would also need the setup explicitly passed [[subagent-stateless-coordinator-passes-context]], whereas a fork inherits it). Fork is CORRECT here (abstract session-state isolation, like d1-q016) - unlike human-handoff-synthesize-not-share-session (d1-q018) where fork is wrong because it is a handoff BETWEEN HUMANS; context (abstract primitive vs human collaboration) decides. META-lesson: when a question NAMES a specific mechanism, pick the option that uses THAT mechanism, not an alternative that merely 'also works'.",
    "seen_in": ["d1-q020"]
  },
  {
    "id": "structured-tool-result-not-plain-text",
    "title": "Strukturni tool_result (tool_use_id), ne plain text - cuva proveniencu akcija<->ishod",
    "domain": 1,
    "lesson_sr": "Rezultat alata vracaj kao STRUKTURNI tool_result blok sa tool_use_id koji se poklapa sa originalnim tool_use, NE kao plain text zalepljen u user poruku. Messages API je strukturisana ACTION-OBSERVATION petlja: tool_use (assistant) -> tool_result (user) sa poklapajucim tool_use_id. Taj id daje PROVENIENCU - model pouzdano mapira svaku opservaciju (rezultat) nazad na tacnu akciju (poziv). Plain text KIDA tu vezu -> model gubi logicku nit, halucinira tool outpute, i ne odrzava stanje (npr. koji portovi su vec skenirani). KLJUC: greska je FORMAT SADRZAJA (nedostaje strukturni blok + tool_use_id metadata), NE role poruke - tool_result tacno ide u USER (ne system). Zamke: tvrdnja da tool data mora u system / da je user samo za coveka (pogresno i inverzno); 'plain text zaobilazi safety filtere' (izmisljeno; degradacija je kognitivna/provenance, ne bypass filtera); krivica na nedostatak is_error flag-a (realan flag, ali oznacava gresku alata, nije uzrok gubitka stanja - realan termin + izmisljena funkcija). Sibling [[batch-parallel-tool-results-one-user-message]] (oba o ispravnoj strukturi tool_result-a: taj pakuje paralelne rezultate u jednu user poruku cuvajuci alternaciju; ovaj cuva svaki rezultat kao strukturni blok cuvajuci tool_use_id proveniencu).",
    "lesson_en": "Return a tool's output as a STRUCTURED tool_result block carrying a tool_use_id that matches the originating tool_use - NOT as plain text appended to a user message. The Messages API is a structured ACTION-OBSERVATION loop: tool_use (assistant) -> tool_result (user) with a matching tool_use_id. That id provides PROVENANCE - the model reliably maps each observation (result) back to the specific action (call). Plain text BREAKS that link -> the model loses the logical thread, hallucinates tool outputs, and fails to maintain state (e.g. which ports were already scanned). KEY: the flaw is the CONTENT FORMAT (missing structured block + tool_use_id metadata), NOT the message role - tool_result correctly goes in a USER message (not system). Traps: claiming tool data must go in system / that user is 'only for humans' (wrong and inverted); 'plain text bypasses safety filters' (invented; degradation is cognitive/provenance, not a filter bypass); blaming a missing is_error flag (real flag, but it marks tool errors, not the cause of state loss - real term + invented function). Sibling of batch-parallel-tool-results-one-user-message (both about correct tool_result structure: that one packs parallel results into one user message preserving alternation; this one keeps each result a structured block preserving tool_use_id provenance).",
    "seen_in": ["d1-q021"]
  }
]
